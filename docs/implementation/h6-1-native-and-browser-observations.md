# H6.1 native and browser observations

Source endpoint: `bc6f607bec5a60df1e72a0d3541156bc9175fe82`. These are actual
recorded actions/results; the chronological ledger contains command details and
intermediate failures. No screenshot file or visual approval is invented.

## E-baseline

Execution/review started on main at `902a7022c895bf97010d979ea578fc5361cdcbf4`.
All supplied 22 package files hash-check; current origin matches the implementation
endpoint with zero ahead/behind and reviewed ancestor retained. Original packages
and additional calibration backups are preserved. Final exports are Arin 24 and
Orin 0, unchanged from the current accepted saves. See report and artifact manifest.
The tracked-source/current-doc audit found no further applicable unclosed Web
marker beyond the retained issue register. Normal smoke covers wider current
language/2D/media/UI/RPG/game paths; an empty GitHub issue list was not used as proof.

## E-camera

`5c2036a`: original unsafe squared-length intermediate is removed by bounded
shared math; native and generated-Web reference tests and full Viewer hardening
pass. Actual native/Chrome scenes and all tabs render and navigate. Solid-ground
orbit is bounded while grid-only underside inspection is retained. Shared pointer
ownership/capture-release and elapsed zoom checks pass. Sin's response to the
requested Desktop/Chrome slow/moderate middle-button orbit, pan, wheel and Show
Gizmo ring test was "It seems to be working". This is user-reported physical input
evidence, not a tool-generated held-middle drag or quantified latency result.

## E-effects

`b8fce49`, `b835628`, `28b78a3`: native and generated-Web lifecycle assertions pass.
Actual native/Chrome frozen equipment hide clears sword/shield mesh, fire and rim;
subsequent actor transforms do not restore hidden parts. Actual Chrome Party
Dragon breath at 1202 ms clears when stepped frozen to 1235 ms and resumes cleanly;
frozen Dragon hide/show has no stale fire. The native/exact-Web fixture covers
forward/backward cuts, clip change, independent other Fire owner, skipped impact
suppression versus one continuous impact cue. Scene-owned advancement/comfort and
independent Fire/Lightning freeze remain intact. Cue counts are cosmetic scheduling,
not combat damage or physical audibility.

## E-actors

`cf84ac8`: two real Orin instances share model resources but use distinct animators,
temporary wrist/node corrections, storm contexts, styles and generation-safe light
leases. Native Auto and forced shader fallback pass; actual Chrome and historical
Edge render the GPU and deliberately forced CPU phases. Freeze/hide, draw, resume,
destroy-one/continue-other, bounded admission, stale lease and complete teardown
assertions match native output. This is not the older effects-disabled mock proof.
Log: `artifacts/temp/h6-1-live-actor-isolation.log`.

## E-rendering

`519e780`, `6b62f3b`, `78e00f4`, `ee2a951`: actual Viewer Arin, Orin, Dragon and
Party render with the screen-fixed backdrop and accepted assets. Web Euler signs
now match native X/Y/Z socket/correction convention: old code produced 30 reference
mismatches, current native/Web normal and PBR fallback pass. Sin reported Arin
looks good and explicitly stopped further screenshot comparison.

Actual Chrome Fire High reports GPU, 5 systems, 399360 GPU bytes, AA4 and error0.
HDR/LDR and AA-only LDR with depth/heat disabled render. Historical Edge additionally
covered Lightning resize and all Viewer tabs. Native/Web post, soft particle and
distortion tests plus explicit GL-double capability/reuse/resize/cleanup checks pass.
Logs: `h6-1-msaa-{final-post-hardening,post-tests,soft-tests,distortion-tests,capability-tests}.log`.

## E-thermal

Web high-level Fire now uses the existing thermal/turbulence GPU service on a
capable WebGL2 path rather than forced CPU Low. Actual reserved-identifier and
transform-feedback binding failures were reproduced and fixed, not hidden by a
fallback. Lightning GPU/Auto permits the native 16384 pool while CPU remains8192
and scene budget32768. Actual historical Edge Ultra reported backend2, pool16384,
3932160 GPU bytes; current Chrome renders Ultra Storm Lance with backend2/GPU.
Actual two-Orin forced CPU fallback and normal native/VM shader/buffer/attribute
fallback checks remain distinct from capable-path proof.

## E-input

Focused canvas keyboard ownership preserves browser/editable shortcuts and clears
held state on blur. Actual Chrome right-click reset suppresses only the canvas
context menu. Fullscreen was verified via Alt+Enter and the focus-revealed Full
Screen button/Enter, including exit. Current `Key_Event_Held` fixes released-
before-poll Ctrl+Arrow: Chrome Defend 550→583→550 ms, H158/V0/zoom-15 unchanged;
native Hit 0→33→0 ms, H234/V0/zoom-15 unchanged. Plain arrow still orbits.
Native real window-procedure fixture and generated-runtime input tests pass.
Logs: `h6-1-key-event-{native,language,viewer-hardening}.log` and earlier
`h6-1-keyboard-focus-test.log`. Native fixture does not inject desktop input.

## E-storage

Native sharing-denial/corrupt/missing/previous-good tests and isolated Viewer
Save/Undo/invalid-JSON tests pass. Generated-Web VM denial/quota/corruption/backup
and fresh-runtime checks pass separately. Actual native/Chrome replacement/Undo
and actual disposable Chrome/Edge storage refresh/tab reopen pass.

At Chrome `http://127.0.0.1:8769/`, Sin selected the distinct 24-key JSON; Codex
confirmed Replace Keys, reloaded, observed Idle frame-zero wrist X35 instead of
packaged X28, downloaded JSON and compared every key/channel by clip name: match.
Canonical/current native and normal browser-origin saves were untouched.
Commit `8457e78`; logs `h6-1-import-native-final.log`,
`h6-1-loader-cache-final-calibration{,-web}.log`. No direct browser D: write,
full browser-process persistence test or real quota/private-mode claim.

## E-lifecycle

`bc6f607`: real `WEBGL_lose_context` originally fatally stopped Viewer during
shader creation. Old runtime fails the added focused regression. Current context
accessor refuses a lost context before/after its queued loss event, and only
resumes resource creation after restore.

One visible Chrome tab was reused for current Viewer/Fire/Lightning on ports
8766/8767/8768 using a disposable HTML observation wrapper. Actual lost/restored
events and running frame sequences: Viewer937/968/1089; Fire1138/1169/1279;
Lightning498/529/628. Viewer showed renderer-unavailable error20, then selecting
Arin and Party successfully reloaded it. An automated Enter did not clear that
overlay. Both Labs automatically resumed visible GPU effects; no page reload.
Fire/Lightning URL-filtered warnings/errors were empty.

Actual user-activated Viewer WebAudio, with scene paused: two real voices started
on channels14/15; actual iframe blur stops both, refocus creates none (starts2,
stops2, active voices2→0→0). AudioContext was running. Corrected shutdown probe
reported stopped/frame4343, closed audio context and zero image refs/cache,
encoded bytes, active SFX, input owners, queues and pointers. Reload rendered again.
These prove scheduling/lifecycle, not speaker output.

The wrapper observes genuine GL/audio functions and exposes visible test buttons;
it is not a GL mock. Earlier wrapper bugs (live-reference snapshots and an uncaught
normal shutdown sentinel) were corrected before the final observations. Old errors
remain in historical tool logs and are not described as a clean complete history.
Source is `artifacts/temp/h6-1-lifecycle.html` (hash in ledger). Its temporary copies
were removed from all publishable outputs. All Chrome windows were then closed
without clearing cookies/history/storage or clicking End Stream; the native Viewer
remained visible and running once. No physical native driver removal was induced;
the existing D3D11 fixture/source recovery-accounting checks are the native baseline.

## E-environment

Actual Windows 11 Pro 10.0.26200; Chrome152.0.7977.77 (later UA still152), historical
Edge146.0.3856.62. WebGL2 reports WebKit WebGL / OpenGL ES3 Chromium. GPU backend
and capability use are observed; the generic string alone is not hardware identity.
Final wrapper outer viewport1463×814, browser DPR approximately0.9, runtime minimum
DPR1, diagnostic iframe1463×595. Its reduced-height UI overlap is not normal layout
acceptance. Earlier normal-window actions/screenshots are separately in the ledger.
Firefox is not passed. No agent Safari/iOS/physical mobile execution. Sin separately
reported the deployed Chrome Viewer on his Mac looks good; not final-build timing
or a complete Mac test matrix.

## E-regression

Normal smoke initial log `h6-1-normal-smoke.log` completed through Phase3B.1, then
found two stale Phase4 test assumptions. `4a30cd3` fixes revoked-object-URL identity
recording and activation timing in the harness without changing expected rendering/
audio output. The exact continuation `h6-1-smoke-continue.cmd` / `.log` completes all
remaining stages, including native/Web demo/no-demo games and final artifacts.
The initial formatter integration/repository-wide style check passed. No repeated
full smoke was added; later scoped checks validate the subsequent changes.

Current normal build, 300 language tests, Viewer hardening (Web included, 58 native
checks), native queued input, GPU normal/fallback and native/Web VFX batches pass.
Logs: `h6-1-context-{build,webgl,vfx}.log`,
`h6-1-key-event-{language,viewer-hardening,native}.log`. Full manual gameplay of all
ordinary games is not claimed; the smoke itself prints that limitation.

## E-builds

Current native Viewer built/launched via Launch.ps1 with SkipWindowActivation;
normal Web rebuilt. Fire/Lightning TargetAll builds pass. Logs:
`h6-1-key-event-viewer-native.log`, `h6-1-context-viewer-web.log`,
`h6-1-context-fire-build.log`, `h6-1-context-lightning-build.log`.
`scripts/install-vsix.cmd` builds, installs and verifies VSIX2.0.59;
`h6-1-context-vsix.log`. Manifest records the actual current output hashes.

## E-scope

G10 is explicitly revised by Sin's Chrome-only instruction; Firefox remains NOT-RUN
and Edge historical only. Gate schema2 represents this without dropping G10 or
other checks. No public Web deployment, future optimized tiers, README rewrite,
new splash/progress guarantee, Double or Battle Scene Editor is claimed here.
