# Phase 5.1 hardening fixture

This focused game validates deep UI style bounds, transactional Menu/Dialogue Class style changes, requested/effective menu rows, multiline text formulas, stale bitmap handles, idempotent facade destruction, and the bounded dialogue-page contract. It runs against both the project and packaged Smile.UI 2.0.0 surfaces.
