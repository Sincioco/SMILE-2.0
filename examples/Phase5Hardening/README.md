# Phase 5.1 hardening fixture

This focused game validates deep UI style bounds, transactional menu/dialogue style changes, requested/effective menu rows, multiline text formulas, stale bitmap handles, and the bounded dialogue-page contract. It runs against both the project and packaged `Smile.UI` 1.0.1 surfaces.
