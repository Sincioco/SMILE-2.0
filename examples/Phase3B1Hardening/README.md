# Phase 3B.1 hardening fixtures

`WebFieldKeys.smile` proves that legal SMILE record field names with JavaScript prototype significance remain ordinary fields in native and Web output.

The generated Web target must use private deterministic keys rather than the source field names.
