# Phase5UIStateTests

This Console project deliberately has no `Game Window`. It proves that a project can reference all of `Smile.UI` and call pure menu-state functions without graphics diagnostics. The package variant proves identical public capability metadata.

The separate invalid fixture calls `Menu.Draw` and must receive exactly one `SML3704` at its call site.
