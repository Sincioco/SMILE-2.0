Option Explicit

Print Visit(3)

End Program

Function Visit(Level As Number) As Text
    Dim Nested As Text

    Select Case "G" + "O"
        Case "GO"
            If Level > 0 Then
                Nested = Visit(Level - 1)
            End If
            Return "Match"
        Case Else
            Return "Miss"
    End Select
End Function
