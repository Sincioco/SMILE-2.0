# Invalid Phase 5 fixtures

Both `ConsoleCallsDraw` project variants must report one source-located `SML3704` at the call site and must not emit a cascade from inside the Smile.UI project or package source.
