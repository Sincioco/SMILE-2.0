# Invalid Phase 3B.1 fixtures

`ModuleCapture` must report `SML3401` for every unqualified project-global type used from the module.

The module must not become valid merely because the consuming application declares `ConsumerData`.
