# Phase 3A Basics

This console proof covers per-source `OPTION EXPLICIT`, typed scalars and arrays, mutable `TEXT`, value copying, concatenation, equality, text `SELECT CASE`, typed returns, default `BYVAL`, explicit `BYREF`, routine locals, and calls through sixteen parameters.

- `Phase3ABasics.smileproj` consumes `Smile.Text.Extras` as a project reference.
- `Phase3ABasics.Package.smileproj` consumes the deterministic package built at `artifacts\libraries\Smile.Text.Extras.smilelib`.
