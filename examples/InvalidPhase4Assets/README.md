# Invalid Phase 4.2 asset fixtures

`MissingExplicit` must fail with `SML3601` at the project XML asset declaration.

`LibraryAsset` must fail with `SML3606` because Phase 4.2 does not package library-owned resources.
