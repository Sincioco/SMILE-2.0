Work only in D:\SMILE 2.0 and use a SINGLE AGENT ONLY.

This package supersedes the earlier Phase 1B package.

Read the repository-root AGENTS.md completely. Then read:

00 - START HERE - Phase 1B Revision 2 Source Visibility File New and Semantic Hardening.md

Read every numbered Markdown file in order and inspect all Repository-Files before editing.

The latest user feedback is authoritative:

- Right-clicking the SMILE project shows “Add new SMILE Source...”.
- The command creates the physical .smile file and opens it for editing.
- The new source does Not appear under the project in Solution Explorer.
- “Add Existing SMILE Source...” then says it is already included.
- Restarting Visual Studio does not make it visible.

Therefore physical creation/opening is not the defect. Project membership apparently succeeds; verify the .smileproj and SmileProjectSourceSet. The blocking failure is Solution Explorer hierarchy visibility and persistence. Do not “fix” it by allowing duplicate project entries or weakening the already-included check.

Implement the exact visible project command name:

New SMILE 2.0 Source Code

Install a real File > New file template named:

SMILE 2.0 Source Code

Rename the existing-file command:

Add Existing SMILE 2.0 Source Code...

Use a focused SMILE project context menu and remove nonfunctional inherited entries such as Connected Services and New EditorConfig File.

Also complete the specified semantic/workspace/governance hardening:
- source-order-independent constants and array dimensions;
- circular constant diagnostics;
- one case-insensitive project-level namespace;
- buffer/callback unregistration;
- multiple project ownership for one physical source;
- root AGENTS.md update.

Preserve native/Web compile and launch, Web sound, breakpoints, F10, Set as Startup, IntelliSense, File > Open, Solution Explorer double-click, Tools > Build SMILE File, DirectX/Direct2D, GDI, and all ten game editions.

Work autonomously through reproduction, implementation, focused tests, scripts\build.cmd, scripts\smoke-test.cmd, refreshed VSIX installation, live Visual Studio validation, commit, and push. Do not ask me to confirm intermediate steps unless genuinely blocked.

The live test must create a source, show it immediately in Solution Explorer, restart Visual Studio and show it again, remove it non-destructively, re-add it, and prove native/Web builds consume it. XML inspection alone is not acceptance.

When mandatory tests are green, inspect all changes, run git diff --check, stage reviewed files with git add -A, commit with a subject beginning exactly “Sin and Codex:”, and push normally. Do not amend, rebase, force-push, or discard user work.

End the report with a prominent bold statement saying whether manual testing is requested from me.
