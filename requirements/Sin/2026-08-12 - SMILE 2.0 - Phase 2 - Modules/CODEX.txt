Work only in D:\SMILE 2.0 and use a SINGLE AGENT ONLY.

Read the repository-root AGENTS.md completely. Then read the extracted Phase 2 package beginning with:

00-START.md

Read every numbered Markdown file in order before editing. Inspect all companion files under repo.

Phase 1 is accepted. Begin Phase 2 now.

Implement the complete Phase 2 modules/imports/reusable-library milestone:

- Module / End Module;
- Import dotted.module As Alias;
- Public and Private module declarations, private by default;
- qualified Alias.Member access;
- shared module/import/visibility semantic model;
- local modules;
- .smilelibproj library projects using the shared SMILE project model;
- deterministic target-neutral .smilelib packages;
- project and package references;
- dependency/build graph and cycle diagnostics;
- Visual Studio SMILE 2.0 Library template and References node;
- add/remove reference UX with immediate refresh;
- public-only module IntelliSense;
- native and Web emission from one bound model;
- project-reference native source debugging;
- proof samples, invalid fixtures, documentation, tests, commit, and push.

Do not implement Type, ByRef/ByVal, general mutable Text, dynamic collections, images/sprites, persistent data blocks, multiple sound channels, Smile.UI.Menu, Smile.RPG.Inventory, Smile.RPG.Abilities, or the RPG in this phase.

Preserve Windows native priority 1, Web priority 2, all ten normal/no-demo games, Web sound, DirectX/Direct2D, GDI, immediate Solution Explorer refresh, File > New, Set as Startup, File > Open, source double-click, breakpoints, and F10.

Use focused happy-path testing, scripts\build.cmd, scripts\smoke-test.cmd, a refreshed VSIX installation, and short live Visual Studio tests. Do not ask me to confirm intermediate steps unless genuinely blocked.

When all mandatory tests are green, inspect all repository changes, run git diff --check, stage all reviewed files with git add -A, create one coherent commit whose subject begins exactly “Sin and Codex:”, and push normally. Do not amend, rebase, force-push, or discard user work.

At the end, prominently state in bold whether any manual testing is requested from me.
