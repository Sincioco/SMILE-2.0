Module Smile.Math.Extras

Public Function IsBetween(Value, MinimumValue, MaximumValue)

    Dim ReturnValue As Boolean

    ReturnValue = Value >= MinimumValue And Value <= MaximumValue
    Return ReturnValue

End Function

End Module
